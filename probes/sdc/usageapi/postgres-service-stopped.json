[
    {
        "name": "postgresql service stopped",
        "type": "log-scan",
        "config": {
            "path": "/var/svc/log/network-postgresql:pg91.log",
            "match": {
                "pattern": "Stopping because"
            }
        }
    }
]
