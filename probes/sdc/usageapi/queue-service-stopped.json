[
    {
        "name": "usageapi-queue service stopped",
        "type": "log-scan",
        "config": {
            "smfServiceName": "usageapi-queue",
            "match": {
                "pattern": "Stopping because"
            }
        }
    }
]
