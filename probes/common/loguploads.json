[
    {
        "name": "logs not uploaded",
        "type": "cmd",
        "config": {
            "cmd": "test ! $(find /var/log/manta/upload -type f | wc -l) -gt 0",
            "interval": 300,
            "threshold": 5,
            "period": 1800
        }
    }
]
